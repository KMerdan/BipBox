DefaultColdStartScanner.scan: validates permission, emits a folder ContextNode, then captures each ScanCandidate from the descender — inspect, enrich metadata, override text.content with composed unit text for aggregates, index into search, embed unless dup, upsert knowledge item + capture event + belongsTo edge. Stable UUIDs derive from the standardized path, so rescans update in place.
