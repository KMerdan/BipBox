Embedding requires the provisioned model: embed() returns nil until the user explicitly downloads it, so items indexed before provisioning have no vector until EmbeddingBackfillService runs (triggered by onBecameReady). The Smart lens has NO fallback — with zero vectors the overview is intentionally empty. Dup-tagged items must never be embedded (the primary copy carries the vector).
