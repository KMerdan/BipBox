TopicDiscovery.discover(vectors:assign:names:...) — mean-center over seeds+assign, cosine kNN over seeds (k=15, edge floor 0.30, parallel), Louvain at resolution 2.0, communities >= 3 become topics with extractive labels (shared stem tokens, CJK-aware, else nearest-centroid title), soft top-3 membership at 0.30 over ALL vectors, symmetrized centroid edges. Pure Swift, no deps; deterministic except Louvain dictionary-order tie-breaks.
