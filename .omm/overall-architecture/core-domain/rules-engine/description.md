Rule-driven organization: RuleDocuments define match conditions and actions, DefaultWorkflowEngine evaluates them, DefaultOperationPlanner plans file moves, and TargetClassifier categorizes destinations. Used for organize-mode intake (drag-drop with routing) rather than index-only scans. Executions go through the FileOperationExecutor port (macOS adapter) so the domain stays side-effect free.
